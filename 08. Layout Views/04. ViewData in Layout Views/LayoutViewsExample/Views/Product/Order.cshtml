@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    // Notice this lifetime of 'ViewData', it will still live in the Layout page according to sequence
    // of rendering of view (ViewImports -> ViewStart -> Controller View -> LayoutView)
    // We use this 'ViewData["Title"]' at _Layout.cshtml
    ViewData["Title"] = "Order";
}

<h1>Order Product</h1>
<p>Order details here</p>